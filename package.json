{
  "name": "rick-and-morty-characters",
  "description": "A list of all characters in Rick And Morty cartoon with a search filter",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "rm -rf dist/* && parcel build src/index.html --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "homepage": "https://codeKameleon.github.io/rick-and-morty-characters",
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.0.1",
    "@parcel/transformer-sass": "^2.0.1",
    "@parcel/transformer-webmanifest": "^2.0.1",
    "parcel": "^2.0.1"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}
